{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB Launch (build/app)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "C: Build"
    }
  ]
}
